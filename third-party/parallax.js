const imageElement = document.getElementById("main-img");
new SimpleParallax(imageElement, {
  scale: 1.6,
  delay: 0.1,
});
// new simpleParallax(imageElement, {
//   scale: 1.6,
//   delay: 0.1,
// });
